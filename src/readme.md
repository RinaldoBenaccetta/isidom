The methods for export and import should be like this :

    export function squareNumber(x) {
        return x * x;
    }
    export var pi = 3.14; 

    import {squareNumber, pi} from "functionsFile";